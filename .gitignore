# macOS
.DS_Store

# Entorno
.venv/
__pycache__/
*.pyc

# Artefactos de entrenamiento de ultralytics (los resultados finales van a models/)
runs/
FinalV2-1/
*.zip

# Pesos base descargados (yolo11m.pt, yolo26m.pt, etc.) — se bajan solos
/yolo*.pt
/rtdetr*.pt

# Descarga completa de SoccerNet (pesada) — solo versionamos el clip elegido
data/soccernet/

# Salidas con frames de SoccerNet dibujados → NDA, no van al repo público
outputs/

# Credenciales
.env
.idea
